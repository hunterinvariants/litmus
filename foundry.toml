# Litmus — runnable PoCs justify every ground-truth label. `forge test` proves the corpus.
[profile.default]
src = "src"
test = "test"
out = "out"
libs = ["lib"]
optimizer = true
optimizer_runs = 200
